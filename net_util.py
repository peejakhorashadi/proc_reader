
# net_util = (rate of traffic for current interval) / (speeed of the ethernet interface) * 100

class NetworkUtilization:
    def __init__(self, denom, num): 
        self.denom = denom
        sefl.num = num


